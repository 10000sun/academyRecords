#함수의 개념 y=fx(y <- 함수(값)) 어떤 값을 입력받아 정해진 계싼을 수행한 후
#그 결과값을 리턴하는 장치

#sort(값), sort(값, decreasing=T) 순서대로 오름차순 내림차순 정렬 
#단, decreasing은 생략가능 sort(값, T)

d <- c(1,7,4,2,3)
sort(d)
sort(d, T)

#paste() 함수는 여러개의 문자열, 숫자값을 하나의 문자열로 만들어줌- paste(a, b, sep=' ')

print(c)

