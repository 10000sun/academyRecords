5>3
2>7
TRUE+TRUE #산술연산에서 TRUE는 1
a <- TRUE
b <- F #TRUE FALSE 앞글자 T, F만 사용가능
addr <- NULL
c <- NA #Not Applicable(누락된 값 저장)
d <- 'NA' #그냥 문자 "NA" 저장
1/0 #양의 무한대
-2/0 #음의 무한대
sqrt(-5) #NaN Not a Number 숫자로 정의가 불가능한 값(addr)
a
b
a+b
c
d